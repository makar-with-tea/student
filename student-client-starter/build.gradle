apply plugin: 'maven-publish'
apply plugin: 'java-library'

dependencies {
    api project(':student-client')
}

publishing {
    publications {
        productStorageClientStarter(MavenPublication) {
            from components.java
        }
    }
}